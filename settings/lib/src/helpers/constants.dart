import 'package:settings/src/helpers/enums.dart';

const themeDisplayTextMap = {
  AdaptiveThemeMode.system: "System Theme",
  AdaptiveThemeMode.dark: "Dark Theme",
  AdaptiveThemeMode.light: "Light Theme",
};
